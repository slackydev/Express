import 'system/threading.xpr' as *

type TMyThread = class(TThread)
  func Execute() print 'Hello world from', GetThreadID()


var myThread := new TMyThread(True)
var i:=0;
while(i < 10) do
  myThread.Start()
  myThread.Join()
  i += 1


print myThread.Terminated()
