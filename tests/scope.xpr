//import 'tests/math.xpr';


func foo();
  var maxDepth: Int = 6
  
  func recurse(depth: Int): Int8;
    print depth.ToStr() + ', '+ maxDepth.ToStr()
    
    if(depth = maxDepth)then
      return maxDepth
    else
      return recurse(depth+1)


  print recurse(0);

  func bar();
    var maxDepth: Int8 = 10;
    
    func recurse(depth: Int): Int8;
      print 'nested recurse'
      if(depth = maxDepth)then
        return maxDepth
      else
        return recurse(depth+1)

    var res := recurse(0);

    print 'recurse gave: '+ res.ToStr();
  
  bar();

foo();