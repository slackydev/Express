{$jit max}

func cmp(ref x,y: int): int
  Result := x-y

func main(); 
  var i: int;
  var arr, tmp: array of int;
  tmp.setlen(300000)
  var n := tmp.len()
  for(i:=0;i<n;i+=1) do tmp[i] := randint(0, $ffffff)
  
  // sort
  arr := tmp.copy()
  var t := MarkTime()
  arr.sort()
  print MarkTime() - t, 'ms!'
  
  // sort weighted test
  arr := tmp.copy()
  var t := MarkTime()
  var weights := arr.copy()
  arr.sort(weights)
  print MarkTime() - t, 'ms!'
  
  // sort lambda test
  arr := tmp.copy()
  var t := MarkTime()
  arr.sort(lambda(ref l,r:int32):int => l-r)
  print MarkTime() - t, 'ms!'

  // sort func test 
  arr := tmp.copy()
  var t := MarkTime()
  arr.sort(cmp)
  print MarkTime() - t, 'ms!'


main()