type TLambda = lambda(): int

var test: TLambda = nil
print test
print 'is nil?: ', test = nil
print 'is nil?: ', nil = test

(*
import 'system/serial.xpr' as *

type TTest = class
  var owner: TSerial;
  
  constructor create(owner: TSerial)
    self.owner := owner;
  
  func Test()
    print (self.owner as TSerial).running
    
    if not (self.owner as TSerial).running then 
      return
    
    (self.owner as TSerial).handle // access Handle:int32


var ser := new TSerial('COM4', 115200)

// sync
print ser.ReadLine()
ser.running := True;
var t := new TTest(ser)
t.Test()

print 'all is good!'
*)